apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "zlifecycle-event-service.fullname" . }}
  labels:
    {{- include "zlifecycle-event-service.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "zlifecycle-event-service.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "zlifecycle-event-service.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "zlifecycle-event-service.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          volumeMounts:
            - name: zlifecycle-event-service-aws-creds
              mountPath: /root/.aws/credentials
              subPath: credentials
              readOnly: true
            - name: zlifecycle-event-service-ssh
              mountPath: /root/.ssh/id_rsa
              subPath: id_rsa
              readOnly: true
          env:
            - name: GIT_TOKEN
              value: {{ .Values.env.GIT_TOKEN }}
            - name: ENABLE_NEW_RELIC
              value: {{ quote .Values.env.ENABLE_NEW_RELIC }}
            - name: NEW_RELIC_API_KEY
              value: {{ .Values.env.NEW_RELIC_API_KEY }}
            - name: ENVIRONMENT
              value: {{ .Values.env.ENVIRONMENT }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      volumes:
        - name: zlifecycle-event-service-aws-creds
          secret:
            secretName: zlifecycle-event-service-aws-creds
        - name: zlifecycle-event-service-ssh
          secret:
            defaultMode: 256
            secretName: zlifecycle-event-service-ssh
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
