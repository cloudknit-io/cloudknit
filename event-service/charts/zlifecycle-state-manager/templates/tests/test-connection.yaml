apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "zlifecycle-event-service.fullname" . }}-test-connection"
  labels:
    {{- include "zlifecycle-event-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "zlifecycle-event-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
