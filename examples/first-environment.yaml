apiVersion: stable.compuzest.com/v1
kind: Environment
metadata:
  name: zmart-checkout-dev
  namespace: zlifecycle
spec:
  teamName: checkout
  envName: dev
  components:
    - name: static-assets
      type: terraform
      module:
        source: aws
        name: s3-bucket
      variables:
        - name: bucket
          value: "zmart-checkout-prod-static-assets"
    - name: static-assets-2
      type: terraform
      dependsOn: [static-assets]
      module:
        source: aws
        name: s3-bucket
      variables:
        - name: bucket
          value: "zmart-checkout-prod-static-assets-2"
